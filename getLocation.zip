import task_manager.db_util

def getLocation(itemName):
    for i in database:
        if(itemName==i) & (inStock(itemName)==true):
            return asile +"#" + section + "#"
               
       else:
           return "Sorry item Out of Stock"
   
